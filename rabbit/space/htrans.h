#ifndef RALGO_HTRANS_H
#define RALGO_HTRANS_H

#include <rabbit/space/htrans2.h>

namespace rabbit
{
	/*template <class T> htrans2<T> rotate(T ang) 
	{
		return htrans2<T>(ang, {0,0});
	};

	template <class T> htrans2<T> translate(linalg::vec<T,2> vec) 
	{
		return htrans2<T>(0, vec);
	};*/
}

#endif